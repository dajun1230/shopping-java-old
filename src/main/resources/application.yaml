# mysql
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shopping-data?useUnicode=true&characterEncoding=UTF-8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

# mybatis
mybatis:
  configuration:
    # 开启下划线自动转驼峰命名
    map-underscore-to-camel-case: true
  mapper-locations: classpath:mapper/*.xml